# ── PyTorch (CUDA 12.1) ─────────────────────────────────────────────────────
# Install separately — must come before everything else:
#   pip install torch==2.5.1+cu121 torchvision==0.20.1+cu121 torchaudio==2.5.1+cu121 \
#       --index-url https://download.pytorch.org/whl/cu121

# ── Unsloth ─────────────────────────────────────────────────────────────────
# Install separately after PyTorch:
#   pip install "unsloth @ git+https://github.com/unslothai/unsloth.git"

# ── Fine-tuning stack ────────────────────────────────────────────────────────
accelerate==1.13.0
bitsandbytes==0.49.2
datasets==4.3.0
peft==0.19.0
transformers==5.5.0
trl==0.24.0

# ── Document generation & augmentation ──────────────────────────────────────
augraphy==8.2.6
Faker==40.13.0
Pillow==12.1.1
reportlab==4.4.10

# ── Analysis & utilities ─────────────────────────────────────────────────────
matplotlib==3.10.8
scikit-learn==1.8.0
scipy==1.17.1
